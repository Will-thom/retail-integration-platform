Suba todos os serviços via Docker Compose:

(altere os *.properties para apontar para rede docker)

docker-compose up --build


Abra o PowerShell na pasta do projeto.
Execute:
.\test-retail-docker.ps1